{
  "name": "qa-full-lifecycle-ui",
  "version": "1.0.0",
  "description": "Playwright UI automation (POM, cross-browser) for the QA full-lifecycle portfolio.",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:report": "playwright show-report",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^26.4.1",
    "eslint": "^9.13.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.11.0"
  }
}
